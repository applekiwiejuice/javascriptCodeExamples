var message = prompt("Write your Message: ");
var charLimit = 280-message.length;
alert("You typed " + message.length + " characters" + ", you stil have " + charLimit + " characters left")
charLimit = 0;
